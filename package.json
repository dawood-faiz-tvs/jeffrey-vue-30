{
  "scripts": {
    "start": "concurrently --kill-others \"npx json-server db/db.json -p 3001\" \"npx serve\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^1.0.0-beta.1"
  }
}
